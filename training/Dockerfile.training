FROM ultralytics/ultralytics:latest

# Install onnx via conda (avoids pip/PyPI network issues)
RUN conda install -y onnx -c conda-forge

# Copy export script from DeepStream-Yolo
COPY DeepStream-Yolo/utils/export_yoloV8.py /ultralytics/

WORKDIR /ultralytics
CMD ["bash"]
